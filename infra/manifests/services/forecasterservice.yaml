apiVersion: apps/v1
kind: Deployment
metadata:
  name: forecasterservice
  namespace: sovereign-genesis
spec:
  selector:
    matchLabels:
      app: forecasterservice
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: forecasterservice
    spec:
      containers:
      - env:
        - name: LOG_LEVEL
          value: INFO
        - name: FORECAST_TO_HISTORY_RATIO
          value: '10'
        - name: KFK_SERVER_ADDRESS
          value: kafka-public:9092
        - name: KAFKA_SERVER
          value: kafka-public:9092
        image: us-central1-docker.pkg.dev/cogctl-gke-v01/sovereign-tfs/forecaster:v3.1-customer
        imagePullPolicy: Always
        name: server
        ports:
        - containerPort: 10040
        - containerPort: 9192
        resources:
          limits:
            cpu: 1000m
            memory: 1024Mi
          requests:
            cpu: 250m
            memory: 128Mi
        startupProbe:
          failureThreshold: 5
          grpc:
            port: 10040
          initialDelaySeconds: 10
          periodSeconds: 1
      initContainers:
      - command:
        - sh
        - -c
        - until nc -z cockroachdb-public 26257 && nc -z nats 4222; do echo
          "waiting for stateful dependencies..."; sleep 2; done;
        env:
        - name: KFK_SERVER_ADDRESS
          value: kafka-public:9092
        - name: KAFKA_SERVER
          value: kafka-public:9092
        image: us-central1-docker.pkg.dev/cogctl-gke-v01/sovereign-tfs/busybox:v3.1-customer
        imagePullPolicy: Always
        name: init-dependencies
      terminationGracePeriodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: forecasterservice
  name: forecasterservice
  namespace: sovereign-genesis
spec:
  ports:
  - name: grpc
    port: 10040
    protocol: TCP
    targetPort: 10040
  - name: metrics
    port: 9192
    protocol: TCP
    targetPort: 9192
  selector:
    app: forecasterservice
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: forecasterservice-hpa
  namespace: sovereign-genesis
spec:
  maxReplicas: 20
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 80
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: forecasterservice
