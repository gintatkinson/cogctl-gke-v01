steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      gcloud container clusters get-credentials "sovereign-genesis-1776197184" --region us-central1
      echo "[DIAG] CONTEXTSERVICE LOGS:"
      kubectl logs -l app=contextservice --all-containers=true --tail=200
      echo "[DIAG] DATABASE STATUS:"
      kubectl get pods -l app=cockroachdb
