steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      gcloud container clusters get-credentials "sovereign-genesis-1776316177" --region us-central1
      echo "=== CONTEXTSERVICE LOGS ==="
      kubectl logs deployment/contextservice --tail=50
      echo "=== NBISERVICE LOGS ==="
      kubectl logs deployment/nbiservice --tail=50
      echo "=== COCKROACHDB STATUS ==="
      kubectl exec cockroachdb-0 -- ./cockroach node status --insecure || true
