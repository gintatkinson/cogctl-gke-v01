apiVersion: apps/v1
kind: Deployment
metadata:
  name: kpi-managerservice
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: kpi-managerservice
  template:
    metadata:
      annotations:
        config.linkerd.io/skip-outbound-ports: '4222'
      labels:
        app: kpi-managerservice
    spec:
      terminationGracePeriodSeconds: 5
      containers:
      - name: server
        image: ${_CUSTOMER_REGISTRY}/kpi_manager:rc13-verified
        imagePullPolicy: Always
        ports:
        - containerPort: 30010
        - containerPort: 9192
        env:
        - name: LOG_LEVEL
          value: INFO
        - name: CRDB_DATABASE
          value: tfs_kpi_mgmt
        - name: CRDB_NAMESPACE
          value: sovereign-genesis
        - name: CRDB_SQL_PORT
          value: '26257'
        envFrom:
        - secretRef:
            name: crdb-data
        resources:
          requests:
            cpu: 250m
            memory: 128Mi
          limits:
            cpu: 1000m
            memory: 1024Mi
      initContainers:
      - name: init-dependencies
        image: busybox:1.36
        command:
        - sh
        - -c
        - until nc -z cockroachdb-public 26257 && nc -z nats-client 4222; do echo
          "waiting for stateful dependencies..."; sleep 2; done;
---
apiVersion: v1
kind: Service
metadata:
  name: kpi-managerservice
  labels:
    app: kpi-managerservice
spec:
  type: ClusterIP
  selector:
    app: kpi-managerservice
  ports:
  - name: grpc
    protocol: TCP
    port: 30010
    targetPort: 30010
  - name: metrics
    protocol: TCP
    port: 9192
    targetPort: 9192
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kpi-managerservice-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kpi-managerservice
  minReplicas: 1
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80
